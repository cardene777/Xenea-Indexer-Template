query domainsReceivedQuery($nodeId: ID!) {
    domainsReceived(nodeId: $nodeId) {
        nodeId
        rindexerId
        contractAddress
        sender
        amount
        txHash
        blockNumber
        blockHash
        network
        txIndex
        logIndex
    }
}