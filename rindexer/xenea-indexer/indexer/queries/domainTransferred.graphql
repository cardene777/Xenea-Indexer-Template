query domainTransferredQuery($nodeId: ID!) {
    domainTransferred(nodeId: $nodeId) {
        nodeId
        rindexerId
        contractAddress
        tokenId
        newOwner
        txHash
        blockNumber
        blockHash
        network
        txIndex
        logIndex
    }
}